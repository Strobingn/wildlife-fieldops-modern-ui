# Wildlife Field Ops — Modern UI

Native Kotlin / Compose field app + web companion.

## Android APK (debug)

Built by GitHub Actions workflow **Build Native Android APK (Debug)**.
Artifact name: `wildlife-field-ops-debug-apk`.

Current branch polish: sticky inspection Dictate → AI Report → AI Estimate bar,
map offline banner/cache UI, restored AiService (`writeInspectionReportFromDictation`),
versionName `2.2.7-inspection-polish` (versionCode 25).
